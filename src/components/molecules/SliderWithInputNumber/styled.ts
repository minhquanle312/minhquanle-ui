// Libraries
import styled from 'styled-components'

// Constants
import { THEME } from 'minhquanle-ui/lib/constants'

export const SliderWithInputNumberWrapper = styled.div`
  font-size: ${THEME.token?.fontSize}px;
`
